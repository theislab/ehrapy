codecov:
  require_ci_to_pass: false

comment: false

coverage:
    status:
        project:
            default:
                target: auto
        patch:
            default:
                enabled: no
    ignore:
        - "tests/"
github_checks:
    annotations: false
